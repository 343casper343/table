import React from "react";
import ReactDOM from "react-dom/client";
import TicketsPanel from "./src/App";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <TicketsPanel />
  </React.StrictMode>,
);
